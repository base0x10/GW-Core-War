- decm-core, decm-core-pair (done?)
- <>{} addressing modes (done?)
- djn.f combine (done?)
- alternative code for arithmetics (add/sub/inc/dec ptr and field)
  - decm - ptr -= coresize -> ptr -= 8 * coresize (done?)

- partial cea eval - add, seq, etc.

- multiple simulator functions (CPUID?)
- double write
- conditional jump reverse evaluation (b-field first, then a field if needed)
- core indices in queue
- one on one warrior special case
- safe math (add/sub, necessary for coresize > field_t / 2)
- void* -> base + int
